name: Actualizar Noticias Diarias

on:
  schedule:
    - cron: '0 8 * * *'  # Se ejecuta todos los días a las 8 AM UTC
  workflow_dispatch:      # Permite ejecutarlo manualmente también

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar repositorio
        uses: actions/checkout@v3

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Instalar dependencias
        run: pip install requests

      - name: ⚙️ Ejecutar script de scraping
        env:
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
        run: python scraper/update_news.py

      - name: 💾 Confirmar y subir cambios
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add public/data/noticias.json scraper/historial
          git commit -m "🔄 Actualización automática de noticias $(date +'%Y-%m-%d')" || echo "No hay cambios"
          git push
